// Generated by CoffeeScript 1.3.3
(function() {

  (function(models) {
    var params;
    params = {
      urlRoot: '/tasks',
      idAttribute: "_id",
      clear: function() {
        return this.destroy();
      },
      toggle: function() {
        var status;
        status = this.get("status");
        if (status === "pending") {
          status = "done";
        } else if (status === "done") {
          status = "pending";
        }
        this.set({
          status: status
        });
        return this.save();
      }
    };
    models.Task = Backbone.Model.extend(params);
  })(app.models);

}).call(this);
